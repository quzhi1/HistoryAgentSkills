---
name: random-history-anecdote
description: 随机提供一个有趣、短小、经史料核验的中国古代历史段子。适合用户说“来个历史段子”“随机历史小故事”“random-history-anecdote”等请求；按译文、出处、出处考证、职官表、原文、meme 推荐的顺序输出，译文直接给正文不写“译文：”，出处考证把辞典解释与我的评价分开，末尾给几个 imgflip 做图链接和每格文案（不给样图），保持简短。不附识典古籍链接和左图右史地图链接，但仍按项目规则换算年号、标注古地名今地，遇官职/爵位/官阶时结合朝代据辞典解释，查不到就重抽或明说。
---

# 随机历史小段子

本 skill 是项目根目录 `SKILL.md` 的轻量入口：目标是让用户快速得到一个有趣的历史小段子，而不是完整历史问答报告。证据底座仍然使用《中国历史大辞典》和 cnkgraph 古籍原文；不得编造段子、不得凭记忆补出处。

## 适用范围

- 中国古代（先秦至清末）人物、事件、言语、轶事、制度运行中的片段，通常不超过 1000 字。
- 每次调用时动态随机发现候选；不使用固定段子池，不限定书目范围，不限定主题词。
- 段子应有现代读者也能感到的趣味：机智、反转、语言梗、人物性格、处世判断、制度荒诞、名实错位等。

不适合：
- 长篇人物传记、完整事件复盘、严肃比较评价题；这些转用根目录 `SKILL.md` 的完整流程。
- 查不到原文或出处的传闻；不要为了有趣而补写。

## 与主 skill 的关系

继承主 skill 的底线：
- 必须真实查询，不得编造原文、出处、译文或背景。
- 原文必须来自本轮查到的辞典或 cnkgraph 片段。
- 出处必须写书名 + 卷/篇/章节名；cnkgraph 只返回卷名时，不要凭空补篇名。
- 译文只翻译原文；不要另写“好玩处”或趣味解释。
- **译文要像当代人说的中文**：不是逐字换词，而是按现代读者的说话方式重写。文言虚词和生僻单字词（「病其人」「稔知」「俾」「既而」「俄顷」「遂」「奈」之类）一律换成日常说法；该拆长句就拆，该补出主语和逻辑连接就补，宁可重组句子也不要留半文半白的腔调。人物对话按现代人开口的语气译。专名、官名、年号、书名照旧保留，不改不译。
- 史料中出现年号纪年，必须运行 `scripts/dynasty_converter.py`，并写成 `年号（公元年）`。
- 最终回答保留古地名时，必须运行 `scripts/place_resolver.py`，并在首次出现处括注今地；查不到就写“今地未能确认”。
- 段子里出现官职、爵位、官阶时，必须查《中国历史大辞典》，结合段子所在朝代解释该头衔；释义只用辞典查到的内容，查不到就明说。

本 skill 的明确例外：
- **不运行、不输出识典古籍链接**。出处行只写书名 + 卷/篇/章节名。
- **不运行、不输出左图右史地图链接**。地点只做现代今地括注。
- 默认不输出“被引史料说明”，除非来源性质会影响理解或用户追问。
- 输出要短；不要展开成长篇史论。
- 唯一允许的外链是末尾 meme 推荐里的 imgflip 做图链接。

## 标准流程

> 命令路径默认 macOS/Linux。Windows PowerShell 可用 `python scripts/run_in_venv.py <script.py|mdict> ...` 或对应 `venv\Scripts\python.exe` / `venv\Scripts\mdict.exe`。

1. 切到项目根目录，即本 skill 目录**真实位置**的上一级。全局安装时本 skill 目录是 `~/.claude/skills/` 下的符号链接，必须先用 `realpath` 解析，不能直接用符号链接的上一级：

```bash
cd "$(dirname "$(realpath ~/.claude/skills/random-history-anecdote)")"
# 已在项目目录时直接：cd /path/to/HistoryAgentSkills
```

```powershell
Set-Location (Split-Path (Get-Item "$env:USERPROFILE\.claude\skills\random-history-anecdote").Target)
```

2. 随机发现候选。

```bash
venv/bin/python scripts/random_anecdote_seed.py --json
```

脚本会在运行时随机生成古汉语检索探针，调用 cnkgraph `Book/Find` 检索全库，随机跳到命中结果页，再返回若干带书名、卷名和上下文的候选片段。它不读取固定段子 JSON。

```bash
venv/bin/python scripts/random_anecdote_seed.py --attempts 15 --candidates 8 --json
```

3. 从返回的 `candidates` 里挑一个完整、有动作或对话、读起来像一则小故事的片段；必要时用候选 `keyword` 或原文中的短句再次查询 cnkgraph，确认上下文。

```bash
venv/bin/python cnkgraph/scripts/query_api.py find --keyword "<候选 keyword 或原文短句>"
```

4. 用《中国历史大辞典》核定人物、书名或关键背景。

```bash
venv/bin/mdict -q "<人物或书名>" dict/历史辞典4合1.mdx
```

5. 查**出处那本书**，做出处考证。用《中国历史大辞典》查这次引用的书名，据词条了解成书年代、作者、性质（正史 / 笔记 / 类书 / 小说等），再据此对这条史料的可信度作一句简短评价。

```bash
venv/bin/mdict -q "<出处书名>" dict/历史辞典4合1.mdx
```

必须把两者分开写：
- **辞典解释**：辞典词条怎么定性这本书，照实转述，不加工。
- **我的评价**：你自己的可信度判断（如正史可信、笔记小说宜存疑、后世类书转引需回溯原书）。

辞典查不到这本书时，如实写"辞典未收此书"，评价只能基于书的性质常识，并标明是推断。转引类书（如《太平广记》《锦绣万花谷》引某书）时，出处以类书为准，可信度按转引处理。

6. 若段子的原文、译文或一句必要背景里出现官职、爵位、官阶名，逐一查《中国历史大辞典》，结合段子所在朝代解释这个头衔。

```bash
venv/bin/mdict -q "<官职/爵位/官阶名>" dict/历史辞典4合1.mdx
```

- **结合朝代取义**：辞典词条常按朝代分列多个义项（如"司空""知制诰"在不同朝代含义差别很大），必须挑与段子朝代对应的一条来解释，不要照搬别的朝代的含义。
- **只说要点、力求短**：一句话说清这个头衔是什么、大致品级或职掌、是实职还是加官/虚衔即可，不展开成职官志。
- **只用辞典查到的内容**：释义必须来自本轮查到的辞典义项；辞典未收该词条时写"辞典未收此职官"，至多据常识作一句谨慎推断并标明"（推断）"，不得当作辞典结论。
- **范围**：官职、爵位（公/侯/伯/子/男等）、官阶/散官、赠官同样处理；只解释段子里真正出现、且现代读者可能不懂的头衔，人人皆懂的（如"皇帝""将军"泛称）可略。

7. 若查到的段子含年号，运行年号换算。

```bash
venv/bin/python scripts/dynasty_converter.py "永乐十五年"
```

8. 若最终原文、译文或一句必要背景里保留古地名，逐一查今地。

```bash
venv/bin/python scripts/place_resolver.py "长安" --year 755 --json
```

9. 若候选查不到可靠原文、出处不清、译文无法确定，最多重抽两次。仍失败时简短说明“这次没有抽到可核验段子”。

10. 配 meme。**不查询、不生成、不附样图**，只做匹配：从下面的固定模板表里挑 2–3 个，写出模板名、做图链接和每格文案，让用户自己去做图。

模板按**段子的结构**挑，不按题材挑：

| 段子结构 | imgflip 模板 slug |
| --- | --- |
| 误解 / 指认错了对象 | `Is-This-A-Pigeon` |
| 一方激动、一方冷淡 | `Woman-Yelling-At-Cat` |
| 四格计划到最后翻车 | `Grus-Plan` |
| 慌 → 稳 → 又慌 | `Panik-Kalm-Panik` |
| 二选一：拒上取下 | `Drake-Hotline-Bling` |
| 两难、都不好选 | `Two-Buttons` |
| 临场急转、临时改主意 | `Left-Exit-12-Off-Ramp` |
| 见新忘旧、临阵换人 | `Distracted-Boyfriend` |
| 上级打断下级胡说 | `Batman-Slapping-Robin` |
| 宁受重罚也不肯做某事 | `UNO-Draw-25-Cards` |
| 提议荒唐、被当场否掉 | `Boardroom-Meeting-Suggestion` |
| 层层升级、越想越离谱 | `Expanding-Brain` |
| 强弱悬殊对比 | `Buff-Doge-vs-Cheems` |
| 歪理自洽、钻空子 | `Roll-Safe-Think-About-It` |
| 自找的后果却大惊失色 | `Surprised-Pikachu` |
| 强颜欢笑、场面话 | `Hide-the-Pain-Harold` |
| 尴尬侧目、无话可说 | `Monkey-Puppet` |
| 真相说出来却被丢掉 | `The-Scroll-Of-Truth` |
| 粗话版 vs 雅言版 | `Tuxedo-Winnie-The-Pooh` |
| 两方达成共识、一拍即合 | `Epic-Handshake` |
| 反复恳求、屡求不应 | `Bernie-I-Am-Once-Again-Asking-For-Your-Support` |
| 肇事者站在一旁看着 | `Disaster-Girl` |
| 苦等、久候不来 | `Waiting-Skeleton` |
| 人人有份、见者都给 | `Oprah-You-Get-A` |

做图链接一律拼成 `https://imgflip.com/memegenerator/<slug>`。表里没有合适结构时，宁可只给一个，也不要临时编 slug——编出来的 slug 会 404。

文案规则：
- 每格一句，尽量短；金句可以直接用原文那一句（如「昨饮酒无偶，聊相要耳」）。
- 只写文字，不描述图片内容，不生成或附样图，不给 `i.imgflip.com` 图片链接。
- 文案要贴这一则段子，不要写成通用鸡汤。

## 输出格式

默认按 **译文 → 出处 → 出处考证 → 职官表 → 原文 → meme 推荐** 的顺序输出；译文直接写正文，不加“译文：”标签。不加查询日志、地图、识典链接，也不另写“好玩处”。

```markdown
**今天的小段子：[短题名]**

[译文正文直接开写，不写“译文：”；用当代汉语说人话，不留文言腔；只翻译原文，不塞分析；涉及古地名时首次出现括注今地；涉及年号时保留年号并括注公元年。]

出处：《[书名]》卷/篇《[章节名]》

出处考证：辞典解释——[辞典对该书的定性，如成书年代、作者、性质]；我的评价——[一句话可信度判断]。

职官表（仅在段子里出现官职/爵位/官阶时才写，逐一列出；否则整段省略）：
- [头衔名]（[朝代]）——[据辞典对应义项的一句话释义；辞典未收则写"辞典未收此职官"]。

原文：「[一段短引]」

meme 推荐（2–3 条，只给做图链接和文案，不附样图）：
- [模板名]（[这条为什么贴：一个短语说结构]）— https://imgflip.com/memegenerator/[Slug]
  [格位]：[文案] ／ [格位]：[文案]

> imgflip 的字体可能不支持中文，出方框就换英文短句，或下载空白图到本地加字。
```

“出处考证”一行必须让读者一眼看出哪句是辞典说的、哪句是你的判断；辞典未收该书时写“辞典解释——辞典未收此书”。“职官表”只在段子真出现官职/爵位/官阶时才出现，没有就整段不写；释义要结合段子朝代取义，不照搬别的朝代含义。“meme 推荐”固定放在最后，只出模板名、做图链接和文案；不附样图、不附 `i.imgflip.com` 图片链接、不自己画图。

## 选材标准

优先：
- 一条原文即可讲完整的小故事；几十字到数百字都可以。
- 1000 字以内都可接受，优先完整性和趣味性；超过 1000 字时才截取完整情节的关键片段，并标明“节引”。
- 有明确人物动作、对话或反转。
- 读者不需要额外解释也能看出趣味。

避免：
- 只有惨烈处刑、灾异、战争死亡数字的材料。
- 需要大段背景才看懂的材料。
- 只在后世笔记中孤证且出处不清的材料；如果使用，必须如实说“仅据某书所载”。
- 把现代段子感强行套到史料上。

## 质量自查

- [ ] 是否真实运行了随机发现脚本，且不是从固定段子池抽取？
- [ ] 是否查到了原文，而不是凭记忆复述？
- [ ] 出处是否包含书名 + 卷/篇/章节名？
- [ ] 输出顺序是否为 译文 → 出处 → 出处考证 → 职官表 → 原文，且译文没写“译文：”标签？
- [ ] 是否查了出处那本书，并给出出处考证？辞典解释与我的评价是否分开、没有混为一谈？
- [ ] 译文是否只翻译原文，没有把分析塞进译文？
- [ ] 译文读起来像不像当代中文？是否还残留文言虚词、生僻单字词或半文半白的句式？
- [ ] 年号是否已换算；无年号则跳过？
- [ ] 古地名是否已查今地；无古地名则跳过？
- [ ] 段子里的官职/爵位/官阶是否已查辞典、并结合朝代解释？辞典未收是否已明说？无此类头衔则跳过？
- [ ] 是否没有输出识典链接、没有输出左图右史地图链接？
- [ ] 原文是否在 1000 字以内，或已对更长材料作完整情节节引？
- [ ] 末尾是否给了 2–3 条 meme 推荐，且 slug 取自 SKILL 里的固定模板表（没有临时编 slug）？
- [ ] meme 推荐是否只给做图链接和文案，没有附样图或 `i.imgflip.com` 图片链接？
- [ ] 是否确实有趣？
